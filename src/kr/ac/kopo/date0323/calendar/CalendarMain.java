package kr.ac.kopo.date0323.calendar;

public class CalendarMain {

	public static void main(String[] args) {
		CalendarIO view = new CalendarIO();
		int menu = view.inputMenu();
		
		while(menu != 3) {
			
			
			if(menu == 1) {
				
				
			} else if(menu == 2) {
//				c.set(view.inputYear(), view.inputMonth(), 1);
			}
		}
		
		
	}
}
